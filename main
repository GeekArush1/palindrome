def palindrome(str):
    c=0
    n=len(str)
    for i in str:
           if(str[i]==str[n-i-1]):
               c=c+1
    if c==len(str):
            print("Palindrome")
    else:
            print("Not a Palindrome")
str=input("Enter a string")
palindrome(str)
